#include "core/disasm.h"

std::string processInstruction(uint8_t *start_addr, uint8_t &increment_amt) {
  std::string instruction = "";

  uint8_t currByte = start_addr[0];

  switch( currByte ) {

  }
  return instruction;
}
