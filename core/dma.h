#ifndef DMA_H
#define DMA_H

#include "defines.h"

/* this is for whenever dma gets impelmented */

PACK(typedef struct dma_state {
    uint64_t dummy;
}) dma_state_t;

extern dma_state_t dma;

#endif
