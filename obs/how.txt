patch pro file :
----------------

comment out the pkgconfig stuff and add this instead:
    # manual build/install of libpng-apng...
    LIBS += -L$$(HOME)/lib -lpng -lz
    INCLUDEPATH += $$(HOME)/include/libpng16

target name in lowercase ("cemu")

for the version, at the top: if (true) { ... }, no git tag fetching
